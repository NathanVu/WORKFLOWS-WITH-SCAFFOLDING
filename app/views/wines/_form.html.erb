<% @wine = wine if @wine.nil? %>
<%= form_for(@wine, remote: true) do |f| %>
  <% if @wine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wine.errors.count, "error") %> prohibited this wine from being saved:</h2>

      <ul>
      <% @wine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :vintage %><br>
    <%= f.number_field :vintage %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <% @wineries_for_select = wineries_for_select if @wineries_for_select.nil? %>
    <%= select(:wine, :winery_id, @wineries_for_select) %>
  </div>
  <div class="actions">
    <%= f.submit 'New Wine'%>
    <button type="button" id="new_wine_button">Test</button>
  </div>
<% end %>
